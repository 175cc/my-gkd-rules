# Copilot Instructions

- Keep each app's GKD rules in its own TypeScript file under `src/apps/`.
- Keep subscription metadata in `src/subscription.ts`.
- Run `pnpm run build` after changing subscription sources.
- Do not edit generated files under `dist/` by hand.
