# Copilot Instructions

- Keep each app's GKD rules in its own file under `src/apps/`.
- Keep subscription metadata in `src/subscription.json5`.
- Run `npm run build` after changing JSON5 sources.
- Do not edit generated files under `dist/` by hand.
